import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Snapcarousel = () => {
  return (
    <View>
      <Text>Snapcarousel</Text>
    </View>
  )
}

export default Snapcarousel

const styles = StyleSheet.create({})